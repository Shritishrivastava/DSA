#include <bits/stdc++.h> 

int sumOfMaxMin(int arr[], int n){
	int max_val = arr[0];
	int min_val = arr[0];

	for(int i = 0; i < n; i++){
		if(arr[i] > max_val){
			max_val = arr[i];
		}
		if(arr[i] < min_val){
			min_val = arr[i];
		}
	}
	return min_val + max_val;
}
