#include <bits/stdc++.h> 
void sort0and1(int n, vector<int> &A){
    
    int left = 0;
    int right = n -1;

    while(left <= right){
        if(A[left] == 0){
            left++;
        }else{
            swap(A[left], A[right]);
            right--;
        }
    }

}
